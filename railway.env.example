# Railway Environment Variables Template
# Copy these variables to your Railway project settings

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database
# Railway will auto-generate this when you add PostgreSQL service

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here
# Generate a random string for production

# Node Environment
NODE_ENV=production

# Port (Railway will set this automatically)
PORT=5000

# Optional: Custom RPC URLs (if you want to use different providers)
# ARBITRUM_RPC_URL=wss://arbitrum-one-rpc.publicnode.com
# BASE_RPC_URL=wss://base.publicnode.com
